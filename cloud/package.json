{
  "name": "temu-monitor-cloud",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Temu 多店监控 · 云端 ingest server (M2 骨架)",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js",
    "seed": "node scripts/seed.js",
    "migrate": "node -e \"import('./db/migrate.js').then(m=>m.migrate()).then(r=>console.log('migrated:', r))\"",
    "backfill": "node scripts/backfill-parsers.js"
  },
  "dependencies": {
    "better-sqlite3": "^12.8.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.0",
    "jsonwebtoken": "^9.0.2"
  }
}
